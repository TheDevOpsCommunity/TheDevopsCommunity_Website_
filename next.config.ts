import type { NextConfig } from "next";

const nextConfig = {
  images: {
    domains: ['www.placehold.co', 'images.unsplash.com', 'randomuser.me', 'via.placeholder.com'],
  },
};


export default nextConfig;
